# essl-sync

Python service that fetches attendance from the eSSL X990 device over Ethernet
and syncs it into the Atteno-Sync Supabase database.

## Setup

1. **Install dependencies**
   ```bash
   cd essl-sync
   pip install -r requirements.txt
